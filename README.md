uv pip list
uv pip install langchain
history
uv init tripplanner
uv pip list
uv python list
uv python install ........
uv venv env --python .....
uv add pandas
conda deactivate
env.scriots/activate.bat
git --version
git init
git config --global user.name "Jasleen Bedi"
git config --global user.email "jasleenbedikaur@gmail.com"
git status